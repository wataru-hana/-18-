# クラウドデプロイ - 5分で完了！

## 🎯 Renderでのデプロイ（最も簡単）

### ステップ1: デプロイ準備（1分）

```bash
cd webapp_example

# configとscrapersフォルダをコピー
./prepare_deploy.sh

# Gitリポジトリを初期化（まだの場合）
git init
git add .
git commit -m "Ready for deployment"
```

### ステップ2: GitHubにアップロード（2分）

1. GitHubで新しいリポジトリを作成
2. 以下のコマンドを実行：

```bash
git branch -M main
git remote add origin https://github.com/あなたのユーザー名/リポジトリ名.git
git push -u origin main
```

### ステップ3: Renderでデプロイ（2分）

1. [render.com](https://render.com)にアクセスしてアカウント作成
2. 「New +」→「Web Service」を選択
3. GitHubリポジトリを選択
4. 設定を入力：

```
Name: price-scraper-app
Build Command: pip install -r requirements_web.txt
Start Command: gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120
```

5. 「Create Web Service」をクリック

### 完了！

約5-10分でデプロイが完了します。表示されたURLからアクセスできます。

---

## 📋 デプロイ後の確認

1. 表示されたURLにアクセス
2. 「実装済み18社の価格を取得」ボタンをクリック
3. スクレイピングが実行されることを確認

---

## ⚠️ トラブルシューティング

### エラー: configフォルダが見つからない

**解決方法**:
```bash
cd webapp_example
./prepare_deploy.sh
git add config scrapers
git commit -m "Add config and scrapers"
git push
```

### デプロイが失敗する

1. Renderの「Logs」タブでエラーを確認
2. `requirements_web.txt`にすべてのパッケージが含まれているか確認
3. `Procfile`が正しく設定されているか確認

---

詳細は `デプロイ手順書.md` を参照してください。



